// Relative Api Paths
const RelativePath = "v1";

const URI = {
  GET_ALL_IMAGES: `${RelativePath}/images/search`,
  GET_IMAGE: `${RelativePath}/images/{image_id}`,
  GET_BREEDS: `${RelativePath}/breeds`,
};

export default URI;
